FROM node:24.0-bookworm-slim AS build
ENV NODE_ENV=development
WORKDIR /app
# Create VERSION and COMMIT_SHA files for development (not using ARGs since dev uses bind mount)
RUN echo "dev" > VERSION
RUN echo "dev" > COMMIT_SHA
COPY package*.json ./
RUN npm ci
